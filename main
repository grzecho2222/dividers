def get_divisors(n):
    divi = set()
    for i in range(1, int(n**0.5) + 1):
        if n % i == 0:
            divi.add(i)
            divi.add(n // i)
    return sorted(divi)

number = 36
print(f"Divisors of {number} are: {get_divisors(number)}")
